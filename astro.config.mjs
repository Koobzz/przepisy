import { defineConfig } from "astro/config";

export default defineConfig({
  // For GitHub project pages, set site to "https://USERNAME.github.io"
  // and base to "/REPOSITORY/". For this repository:
  site: "https://koobzz.github.io",
  base: "/przepisy/",
});
